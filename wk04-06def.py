find and use functions from within modules
for example module Math contains functions that are related to Math such as square root and factorial

values, also known as objects, can contain functions
functions inside of objects are called methods
form of method call: Object.method(arguments)

To use a function that has been defined inside module Math,, we first import math
